/* "export default { extends: ['@commitlint/config-conventional'] };"; */
// commitlint.config.js
module.exports = {
    extends: ['@commitlint/config-conventional'],
    rules: {
      // Add any custom rules if needed, e.g.,
      // 'scope-enum': [2, 'always', ['core', 'docs', 'ui']]
    },
  };
  
